# Vulnerable demo repository

This repository intentionally contains a synthetic AWS access key so the
SecretScope demo has something to find. The key is a well-known documentation
placeholder and cannot authenticate against AWS.
